<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <import type="com.example.core_ui.util.DoubleConverter" />

        <variable
            name="item"
            type="com.example.yummy.search.presentation.FoodIngredient" />

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:weightSum="10">

        <EditText
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_weight="2"
            android:focusable="true"
            android:focusableInTouchMode="true"
            android:hint="@string/search_ingredient_name_hint"
            android:importantForAutofill="no"
            android:inputType="text"
            android:singleLine="true"
            android:text="@={item.description}" />

        <com.example.widget.edittext.amount.AmountEditText
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_weight="8"
            android:importantForAutofill="no"
            android:singleLine="true"
            android:text="@={DoubleConverter.doubleToString(item.amount)}" />

    </LinearLayout>

</layout>