<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Common widget attributes -->
    <attr name="text" format="reference" />
    <attr name="image" format="reference" />

    <!-- ImageWithLabel -->
    <declare-styleable name="ImageWithLabel">
        <attr name="text" />
        <attr name="image" />
    </declare-styleable>

    <!-- TextSection -->
    <declare-styleable name="TextSection">
        <attr name="text" />
    </declare-styleable>

    <!-- ProgressButton -->
    <declare-styleable name="ProgressButton">
        <attr name="text" />
    </declare-styleable>

    <!-- Toolbar -->
    <declare-styleable name="Toolbar">
        <attr name="text" />
    </declare-styleable>


</resources>